name = "lane-logic"
description = "Unit-lane worker for approved Paper tasks; isolated worktree, red test first, no host runtime."
sandbox_mode = "workspace-write"
developer_instructions = """
Work only on task ids and {files:} globs handed to you by the parent. Read the project profile, feature SPEC, plan, CODEMAP, and the relevant .agents/skills before editing. Write a failing unit test from the SPEC first, then the smallest implementation. Do not run a host, tick plans, commit, push, or edit outside the assigned globs. Return one evidence row per task with the exact test command and result. The parent session owns lifecycle, git, and final verification.
"""

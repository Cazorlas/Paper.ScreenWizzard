# Paper.ScreenWizzard đợt 1: khung, chụp, sửa ảnh — plan — 2026-09-20

**Trạng thái:** đã duyệt 2026-09-20 ("mình duyệt nha")
**Loại việc:** code

Dựng solution .NET 10 bốn tầng (ADR 0001, phương án A) và làm ba tính năng đầu: khung ứng dụng (khay, phím tắt, cài đặt),
chụp màn hình bốn kiểu, trình sửa ảnh. Không host nào: thứ chạy thật là chính file exe trên desktop tương tác.
Brief: [2026-09-20-m1-chup-va-sua-anh.md](2026-09-20-m1-chup-va-sua-anh.md) · Luật: [SPEC.md](SPEC.md) (shell),
[../capture/SPEC.md](../capture/SPEC.md), [../editor/SPEC.md](../editor/SPEC.md)

## Context

- Kho trống. Mọi thứ dưới đây là mới. Tầng và target: xem `CLAUDE.md` và ADR 0001.
- Kho tham khảo: PaperTodo (WPF, một app nhỏ giữ cửa sổ nổi và lưu vị trí) chỉ để học cách bố cục; Fluent-Screen-Recorder
  dành cho đợt 2. Không chép mã của kho nào.
- **`PaperLibrary` (RevitAPI-C) không dùng được:** nó là project trong solution Revit, không phải gói NuGet, và ràng buộc
  "không tham chiếu mã nguồn dự án khác" của kho này cấm liên kết file. Vì vậy `.App/Mvvm/` tự giữ `BindableBase`,
  `CommandBase`, `AsyncCommandBase` nhỏ, cùng tên và hình dạng như trong `paper-wpf-style`, để sau này đổi sang
  thư viện dùng chung nếu nó được tách thành gói.
- **Dịch vụ dùng chung:**
  - transaction/undo: **cần**, là lịch sử của trình sửa ảnh (UseCases/Editor), tự làm trong T11-T12, không phải dịch vụ chung;
  - progress có huỷ: **không cần** ở đợt này (ảnh chụp tức thì);
  - thông báo/báo cáo: **thiếu** → T2 khai cổng thông báo (`INotificationPort`: dòng báo nhỏ và hộp lỗi), T6 cài;
  - lưu cài đặt: **thiếu** → T2 khai `ISettingsStorePort`, T17 cài `%AppData%\Paper\ScreenWizzard\configs\settings.json`;
  - xử lý lỗi/cảnh báo: mỗi tính năng trả kết quả có tên lỗi (F-code), ViewModel hiện; không ném ngoại lệ ra UI;
  - logging: **thiếu** → T2 khai `ILogPort`, T17 cài ghi file `%AppData%\Paper\ScreenWizzard\logs\`.
- **Bài học đã nhận áp dụng:** không có (kho mới, sổ `retro` chưa có).
- **Bug liên quan:** không có.

## Rules that apply

- ADR 0001: quyết định ở UseCases/Domain, host chỉ qua port, dữ liệu qua port là dữ liệu thuần (mảng byte pixel BGRA,
  toạ độ nguyên pixel vật lý, không có `System.Windows`/`System.Drawing` trong hai tầng đó).
- `CLAUDE.md`: property/field private `_camelCase`; lệnh là class có tên; chữ hiển thị qua `DynamicResource`.
- Mỗi test bắt đầu bằng mã `F<n>` của dòng nó phủ; lớp test đặt theo tính năng nên `F2` của chụp và `F2` của khung không
  lẫn nhau (task ghi rõ `capture/F2`).
- `paper-wpf-style`: tương phản 4.5:1 chữ và 3:1 biểu tượng ở cả hai giao diện, viền focus rõ, biểu tượng có tên, dùng
  được bằng bàn phím, tôn trọng cài đặt tắt hiệu ứng.
- Gói NuGet: bản mới nhất mà `net10.0` cho phép, kiểm bằng `dotnet list package --outdated` ở T1.

## Decisions

- **Solution `.slnx`, bốn project src và ba project test** — `Domain`, `UseCases` (`net10.0`), `Infrastructure`, `App`
  (`net10.0-windows10.0.19041.0`, WinExe); `UnitTests` (`net10.0`), `UiTests`, `E2eTests` (`-windows`). Rejected:
  Presentation thành project thứ năm, vì compiler đã giữ ranh giới còn lại (ADR 0001).
- **Test bằng NUnit 4** — theo Paper.AutoCad, để một cách viết test cho mọi dự án Paper. Rejected: xUnit, không có lý do
  khác biệt đủ để tách.
- **Toạ độ luôn là pixel vật lý của desktop ảo, process per-monitor DPI v2 (khai trong manifest)** — chụp và chọn cùng một
  hệ toạ độ, nên không có bước đổi đơn vị nào để sai. WPF làm việc bằng đơn vị hiển thị, nên lớp phủ chọn vùng đổi ở đúng
  một chỗ (một hàm trong App) và có test.
- **Chụp bằng GDI `BitBlt` từ desktop ảo, một lần lúc bấm (hay hết đếm ngược); lớp phủ hiện ảnh đó** — ảnh đứng yên trong
  lúc chọn, và ảnh ra không bao giờ chứa lớp phủ (SPEC chụp: F-rows và dòng "Ảnh không bao giờ chứa…"). Rejected:
  Windows.Graphics.Capture cho ảnh tĩnh, vì nó dành cho quay, cần thêm WinRT và chậm khởi động; sẽ dùng ở đợt 2.
- **Khung cửa sổ = `DWMWA_EXTENDED_FRAME_BOUNDS`, không phải `GetWindowRect`** — để ảnh không dính viền bóng vô hình
  (SPEC chụp, Cửa sổ). Danh sách cửa sổ chụp cùng lúc với ảnh đóng băng.
- **Trình sửa ảnh giữ hình vẽ là danh sách vật thể, ghép vào pixel chỉ lúc lưu / chép** — chọn, kéo, đổi màu, xoá được
  cho tới lúc lưu (SPEC sửa). Vẽ ra pixel bằng WPF `RenderTargetBitmap` ở App (cần font và bút), còn **mọi luật**
  (lịch sử, đánh số, ghép ô vuông làm mờ, phép cắt, ràng buộc Shift) ở UseCases/Domain trên số nguyên và mảng byte.
  Rejected: vẽ thẳng lên bitmap ngay từ đầu, vì không sửa lại được và Undo phải lưu cả ảnh.
- **Làm mờ = ghép ô vuông 12 px** — không đảo ngược được (SPEC sửa, Làm mờ).
- **Sau khi chụp là một hộp thoại chọn nơi, không mở thẳng trình sửa** — Hùng nói 2026-09-20 (theo FastStone). Quyết định
  "ảnh đi đâu" thuộc UseCases: interactor chụp trả về một **plan** (mở hộp thoại, hay đi thẳng tới trình sửa/clipboard/file
  theo cài đặt), rồi mỗi nút của hộp thoại gọi một lệnh của interactor và nhận kết quả có mã lỗi (capture/F4, F5).
  Hộp thoại giữ ảnh của riêng nó, nên nhiều hộp cùng mở được. Rejected: hộp thoại tự gọi cổng file và clipboard, vì lúc đó
  F4 và F5 chỉ kiểm được khi có Windows.
- **Mỗi phím tắt toàn cục qua `RegisterHotKey`** — không hook bàn phím toàn cục, không cần quyền cao, và phím đã bị ứng
  dụng khác giữ báo lỗi ngay (khung F4, chụp F1).
- **Tray bằng `System.Windows.Forms.NotifyIcon`** trong `App` — WPF không có khay sẵn; chỉ dùng lớp này, không dùng
  WinForms cho việc khác. Rejected: gói bên thứ ba cho một biểu tượng khay.
- **Ngôn ngữ: hai `ResourceDictionary` vi/en đổi được lúc chạy**, khoá dùng chung cho cả ba tính năng.
- **Đợt 2 (quay) và 3 (ghi chú) chưa có SPEC**; xem `docs/roadmap.md`.
- **Sửa hình thức 2026-09-20 khi làm T1 (không đổi luật):** glob của T1 thêm `App.xaml` và `App.xaml.cs`, vì một exe WPF không
  có `Application` thì không build được; T1 chỉ dựng bản tối thiểu (không cửa sổ, tắt khi gọi `Shutdown`), T17 mở rộng nó.
- **Sửa hình thức 2026-09-20 khi làm T1 (không đổi luật):** bỏ lane `e2e`. Kit từ chối profile khai lane `e2e` với host `desktop`
  ("lane cannot be proved by any declared host"): host này chứng minh được lane `ui`. T16-T18 đổi thẻ từ `[e2e]` sang `[ui]`,
  vẫn ở nhóm 5 riêng (không cùng nhóm với ui khác, vì cùng cần màn hình) và vẫn chạy verb `e2e` trên project `E2eTests`.
  Rejected: khai thêm host `cli` chỉ để có lane `e2e` — nó mang theo kho công cụ của chính kit mà app này không dùng.
- **Presentation là project riêng (2026-09-20, sau khi Hùng hỏi "bạn ko làm tầng Presentation hả"):** ADR 0001 chọn phương án A, mà phương án A liệt kê Presentation là một project; lúc T1 mình để nó thành thư mục trong `App` với lý do "compiler đã giữ ranh giới còn lại" — lý do sai, vì ranh giới Presentation/Infrastructure khi đó chỉ do hook và test giữ, và đó là một ngoại lệ mà không ai duyệt. T23 tách project trước khi làm nhóm 4; T14 và các task sau dùng đường dẫn `src/Paper.ScreenWizzard.Presentation/**`. T6 và T10 đã tick giữ nguyên đường dẫn `App/...` của lúc chúng chạy (không viết lại task đã tick); T23 chuyển file đó. Rejected: để nguyên và sửa ADR thành "Presentation là thư mục" — trái yêu cầu của Hùng và mất ranh giới compiler.
- **Quyết định của lane ui ở T10, 2026-09-20:** `Capture.DisplayChanged` do App chọn (`CaptureMessages`) vì `CheckDisplayUnchanged` chỉ trả `CaptureIssue`; kết quả `Delivered=false` mà `KeepDialogOpen=false` vẫn **giữ hộp thoại mở** với lỗi bên trong, để ảnh không bao giờ mất im lặng (ý định của interactor không được nêu); giao tự động không hộp thoại mà lỗi thì hiện hộp lỗi; hộp thoại thứ hai trở đi lệch 32 px. Các trang docs đã đọc: `SetWindowPos`, `GetWindowRect`, `WindowInteropHelper.EnsureHandle`, `VisualTreeHelper.GetDpi`, `DpiScale`, `Window.OnDpiChanged`, `SystemParameters.ClientAreaAnimation` (cũng trả phần nợ của lane shell cho mục này), `BitmapSource.Create`, `PixelFormats.Bgra32`, `UIElement.CaptureMouse`, `SaveFileDialog`, `Progress<T>`. **Còn nợ tra:** `Window.Close` (trang mỏng, việc đóng cửa sổ đã đóng ném lỗi là từ trí nhớ), `Topmost`/`ShowInTaskbar`/`ShowActivated`/`ResizeMode`/`SizeToContent`/`WindowStartupLocation`/`Activate`, `FocusManager`, `StreamGeometry`, `DoubleAnimation`, `RenderTargetBitmap`, và phần shell đã nêu; sẽ trả trước `/task-verify`.
- **Sửa hình thức 2026-09-20 khi vào nhóm 3 (không đổi luật):** glob của T8 thêm `UseCases/Common/Implements/**` và `Domain/Common/**`, vì quy tắc đặt tên file (`Screenshot yyyy-MM-dd HH.mm.ss`, `(2)`, `(3)` không ghi đè), nền trắng của JPG và ghi file / clipboard nằm ở `ImageDelivery` dùng chung với trình sửa (T12 chỉ gọi nó, không sửa nó).
- **Quyết định của lane ui ở T6, 2026-09-20:** Cài đặt dàn **hai cột** (một cột cao ~1100 px, phải cuộn trên màn hình 1080p), nên thứ tự Tab là cột trái từ trên xuống rồi cột phải, và "Khởi động cùng Windows" nằm trước "Lưu file" so với wireframe; thanh chụp có thêm nút ✕ (SPEC nói "bấm X trên thanh chụp", wireframe không vẽ); mỗi nhóm nút radio là **một** điểm dừng Tab, đổi bằng phím mũi tên. Lane ui viết bằng kiến thức các thành viên WPF/Win32 sau (`Window.DragMove`, `SystemParameters.ClientAreaAnimation`, `DispatcherTimer`, `KeyEventArgs.SystemKey`, `Keyboard.Modifiers`, `Microsoft.Win32.OpenFolderDialog`, `Registry` khoá `AppsUseLightTheme`, `DwmSetWindowAttribute` với `DWMWA_USE_IMMERSIVE_DARK_MODE`) mà **chưa mở trang docs**: trái quy trình "tra trước khi gọi"; nợ đó được xếp vào phần T15 còn mở và phải trả trước `/task-verify`.
- **Khung F2 cần đường dẫn (phát hiện ở T4, 2026-09-20):** thông báo "không lưu được cài đặt" phải nêu đường dẫn (SPEC shell F2) nhưng
  `ShellInteractor` chỉ nhận `PortResult.Detail`. Không đổi hợp đồng: adapter lưu cài đặt (T17) phải đặt **đường dẫn đầy đủ vào Detail**
  (`"<path>: <lý do hệ thống>"`), và T16 có test kiểm điều đó; lời "thay đổi mất khi thoát" nằm trong chuỗi `Shell.SettingsNotSaved` của lane ui.
- **Phát hiện khi tra API (T15), 2026-09-20:** trang `RegisterHotKey` nói F12 luôn dành cho trình gỡ lỗi, không nên đăng ký. SPEC shell cho
  phép phím chức năng đứng riêng mà không nhắc F12. Không đổi luật: adapter T17 từ chối F12 như một phím "ứng dụng khác đang giữ", đi qua
  đúng dòng shell/F4 đã có (thông báo nêu phím và lý do, phím cũ giữ nguyên). Cùng trang: hotkey cùng id không tự bị thay, nên cổng đăng ký
  id mới trước rồi mới gỡ id cũ, giữ được tính nguyên tử mà `IHotkeyPort` đã hứa; và PrintScreen có thể đăng ký được dù Snipping Tool đang
  dùng, nên T18 phải thử thật phím này.
- **Sửa hình thức 2026-09-20 khi làm T2 (không đổi luật):** T5 thêm glob `tests/Paper.ScreenWizzard.UiTests/Support/**` (bộ điều khiển dùng chung: luồng STA, FlaUI gắn theo handle, chụp ảnh), T10 và T14 thêm `src/Paper.ScreenWizzard.App/Resources/**` để mỗi tính năng thêm chuỗi vi/en của mình vào từ điển dùng chung mà T6 dựng; hai nhóm khác nhau nên không giao nhau. T2 thêm bộ khung interactor trả giá trị rỗng để test đỏ ở assertion; bổ sung so với hợp đồng dự kiến: `IShellInteractor.CreateDefaultSettings` (mặc định của bảng Inputs là luật cần test) và `ICaptureInteractor.RequestFor` / `PlanAfterCapture`.

## UI wireframe

```text
Thanh chụp (luôn trên cùng, kéo được)         Menu khay
┌──────────────────────────────────────┐      ┌───────────────────────┐
│ [▭ Vùng] [✎ Tự do] [▢ Cửa sổ] [⛶ Cả màn] │ [⚙] │ Chụp vùng chữ nhật  PrtScn │
└──────────────────────────────────────┘      │ Chụp vùng tự do   Shift+ … │
                                              │ Chụp cửa sổ       Alt+ …   │
Lớp phủ chọn vùng (mọi màn hình, ảnh đóng băng) │ Chụp toàn màn hình Ctrl+ … │
┌────────────────────────────────────────┐    │ Mở ảnh…                    │
│  (ảnh đóng băng, mờ 40%)                  │    │ Thanh chụp   ✓             │
│      ┌─────────────┐                    │    │ Cài đặt                    │
│      │ vùng sáng    │ 412 × 268         │    │ Thoát                      │
│      └─────────────┘                    │    └───────────────────────┘
│  Esc / chuột phải: huỷ                   │
└────────────────────────────────────────┘

Hộp thoại "Đã chụp" (luôn trên cùng, giữa màn hình vừa chụp; mỗi lần chụp một hộp riêng)
┌────────────────────────────────────────────┐
│ Đã chụp                                  ✕ │
├────────────────────────────────────────────┤
│  ┌──────────────────────┐   1280 × 720     │
│  │  ảnh thu nhỏ          │   PNG             │
│  └──────────────────────┘                  │
│  [Lưu] [Lưu thành…] [Sao chép] [Sửa] [Bỏ]  │
└────────────────────────────────────────────┘

Trình sửa ảnh
┌──────────────────────────────────────────────────────────────────────┐
│ [Chọn][Bút][Dạ quang][Đường][Mũi tên][Khung][Elip][Chữ][1][Mờ][Cắt] │
│ Màu ●●●●●●●● [+]  Dày ▬▬▬ 4   Chữ 18       [↶][↷]  [Vừa khung] 100%  │
├──────────────────────────────────────────────────────────────────────┤
│                    (ảnh + các hình vẽ, cuộn / phóng to)               │
├──────────────────────────────────────────────────────────────────────┤
│ 1280 × 720   Chưa lưu                              [Sao chép] [Lưu…] │
└──────────────────────────────────────────────────────────────────────┘

Cài đặt: các nhóm Phím tắt · Sau khi chụp · Lưu file · Khởi động cùng Windows · Ngôn ngữ · Giao diện; Enter lưu, Esc đóng.
```

## Tasks

Thứ tự: test đỏ trước code trong mỗi lane; mock UI trước UI thật và trước chạy thật; các task chạy thật (verb `e2e`, thẻ `[ui]`) nằm ở nhóm 5, sau các nhóm `ui` khác,
và không cùng nhóm với chúng (cùng cần màn hình); `find-bug`, review kiến trúc và đóng SPEC cuối. Lane trong một nhóm chạy
song song, mỗi lane một lane agent, chỉ sửa file trong `{files:}` của task mình. Mã `shell/F<n>`, `capture/F<n>`,
`editor/F<n>` là dòng của bảng "When it does not do the job" ở SPEC tương ứng; mỗi mã một test bắt đầu bằng `F<n>_`.

### 1. Khung solution và hợp đồng — một luồng, chưa có lane song song

- [x] T1 Dựng solution `Paper.ScreenWizzard.slnx`, bốn project src và ba project test (không có logic), `Directory.Build.props` (`net10.0`, nullable, warning là lỗi), `.gitignore`, `.editorconfig`, thư mục `Mvvm` của App; kiểm bản mới nhất của gói NuGet; khai verbs `build`, `test`, `ui`, `e2e` vào `.claude/paper.profile.json` (kèm `verify.watch`, `featureDocs`, `docsSource`, `architecture.platformFree` cho Domain và UseCases, `api.namespaces` cho `System.Windows`, `Windows.` và `Microsoft.Win32`) — xong khi verb `build` exit 0 và `git status` không có file sinh ra ngoài `.gitignore` {files: Paper.ScreenWizzard.slnx, Directory.Build.props, Directory.Build.targets, .gitignore, .editorconfig, src/**/*.csproj, tests/**/*.csproj, src/Paper.ScreenWizzard.App/Mvvm/**, src/Paper.ScreenWizzard.App/app.manifest, src/Paper.ScreenWizzard.App/App.xaml, src/Paper.ScreenWizzard.App/App.xaml.cs, .claude/paper.profile.json}
- [x] T2 Hợp đồng của ba tính năng, chỉ interface, record thuần và **bộ khung interactor trả giá trị rỗng** để T3, T7, T11 viết được test đỏ ở assertion chứ không đỏ vì build (không logic): cổng `IScreenSourcePort`, `IWindowCatalogPort`, `IMonitorCatalogPort`, `IClipboardPort`, `IFileStorePort`, `IHotkeyPort`, `ISettingsStorePort`, `IAutostartPort`, `INotificationPort`, `ILogPort`, `IClockPort`; interactor `ICaptureInteractor`, `IEditorInteractor`, `IShellInteractor`; record vào/ra (vùng, đường bao, cửa sổ, kết quả có mã lỗi F); kiểu giá trị Domain (điểm, hình chữ nhật, màu) — xong khi verb `build` exit 0 và không interface nào nhắc kiểu của `System.Windows` hay `System.Drawing` (sau T1) {files: src/Paper.ScreenWizzard.Domain/**, src/Paper.ScreenWizzard.UseCases/**/Ports/**, src/Paper.ScreenWizzard.UseCases/**/Models/**, src/Paper.ScreenWizzard.UseCases/**/Implements/**}

### 2. Khung ứng dụng (shell) — logic và giao diện song song

- [x] T3 [red][unit] Test cho SPEC shell: "Phím tắt" (chữ đơn A bị từ chối, Ctrl+Shift+A nhận, PrintScreen và F9 đứng riêng nhận, phím trùng kiểu chụp khác bị từ chối kèm tên, phím ứng dụng khác giữ bị từ chối và phím cũ giữ nguyên, đổi phím có hiệu lực ngay), "Cài đặt được giữ lại" (thư mục, vị trí thanh chụp ngoài màn hình → góc trên phải màn hình chính, file hỏng → mặc định cộng .bak), "Khởi động cùng Windows", "Ngôn ngữ và giao diện" (chọn vi/en theo culture), quyết định "Chỉ một bản chạy"; F1 F2 F3 F4 F5 F6 F7 của shell — xong khi đỏ ở **assertion**, không đỏ vì build {files: tests/Paper.ScreenWizzard.UnitTests/Shell/**}
- [x] T4 [unit] Code Domain/UseCases của shell tới khi T3 xanh; verb `test` — xong khi exit 0 và số test đã chạy > 0 {files: src/Paper.ScreenWizzard.UseCases/Shell/Implements/**, src/Paper.ScreenWizzard.Domain/Shell/**, tests/Paper.ScreenWizzard.UnitTests/Shell/**}
- [x] T5 [red][ui] Mock UI trên dữ liệu giả theo wireframe: thanh chụp, cửa sổ Cài đặt, menu khay (nội dung), thông báo; SPEC shell "Dùng được bằng bàn phím" (thứ tự Tab, Enter lưu, Esc bỏ, tên cho nút biểu tượng), "Ngôn ngữ và giao diện" (đổi chữ ngay, tương phản 4.5:1 và 3:1 ở cả hai giao diện), shell F4 F5 hiện thông báo — xong khi đỏ ở assertion {files: tests/Paper.ScreenWizzard.UiTests/Shell/**, tests/Paper.ScreenWizzard.UiTests/Support/**}
- [x] T6 [ui] View và ViewModel của thanh chụp, Cài đặt, menu khay, cài đặt của cổng thông báo; chạy verb `ui`, **mở ảnh sáng và tối ra xem** — xong khi T5 xanh và ảnh khớp wireframe {files: src/Paper.ScreenWizzard.App/Views/Shell/**, src/Paper.ScreenWizzard.App/ViewModels/Shell/**, src/Paper.ScreenWizzard.App/Resources/**, tests/Paper.ScreenWizzard.UiTests/Shell/**}

### 3. Chụp màn hình — logic và giao diện song song

- [x] T7 [red][unit] Test cho SPEC capture: "Vùng chữ nhật" (kéo ngược hướng, cắt theo mép, toạ độ âm hợp lệ, hai màn hình liền, pixel thật ở 150%), "Vùng tự do" (hình bao, trong/ngoài đường bao, đường chưa khép), "Cửa sổ" (trên cùng, bỏ ẩn/thu nhỏ/lớp phủ, nền desktop → cả màn hình, khung thấy được không có viền bóng, cửa sổ phóng to), "Toàn màn hình" (màn hình có con trỏ, tất cả), "Độ trễ và con trỏ", "Sau khi chụp" (hộp thoại là mặc định và chưa ghi gì; Sao chép, Lưu, Lưu thành… huỷ thì quay về hộp thoại, Sửa, Bỏ; nhiều hộp thoại độc lập; cài đặt tự đi thẳng tới clipboard hay trình sửa thì không có hộp thoại; đặt tên, (2) (3) không ghi đè, JPG nền trắng, PNG giữ trong suốt), và capture/F2 F3 F4 F5 F6 F8 F9 F10 (F1 và F7 ở nhóm 5) — xong khi đỏ ở assertion {files: tests/Paper.ScreenWizzard.UnitTests/Capture/**}
- [x] T8 [unit] Code UseCases/Domain của capture tới khi T7 xanh; verb `test` — xong khi exit 0 và số test đã chạy > 0 {files: src/Paper.ScreenWizzard.UseCases/Capture/Implements/**, src/Paper.ScreenWizzard.UseCases/Common/Implements/**, src/Paper.ScreenWizzard.Domain/Capture/**, src/Paper.ScreenWizzard.Domain/Common/**, tests/Paper.ScreenWizzard.UnitTests/Capture/**}
- [x] T9 [red][ui] Mock lớp phủ chọn vùng trên **ảnh đóng băng giả**: kéo chữ nhật hiện "rộng × cao", vẽ tay vùng tự do, cửa sổ sáng viền và tên, Esc và chuột phải huỷ, đếm ngược 5 tới 1, capture/F2 và F3 hiện "vùng quá nhỏ" và ở lại màn chọn, đổi đơn vị hiển thị → pixel ở đúng một hàm, **hộp thoại "Đã chụp"** (ảnh thu nhỏ, kích thước, năm nút có tên, Esc = Bỏ, capture/F4 và F5 hiện lỗi mà hộp vẫn mở, Tab và viền focus, hai giao diện) — xong khi đỏ ở assertion {files: tests/Paper.ScreenWizzard.UiTests/Capture/**}
- [x] T10 [ui] View và ViewModel của lớp phủ chọn vùng, đếm ngược và hộp thoại "Đã chụp"; verb `ui`, **mở ảnh ra xem** — xong khi T9 xanh và ảnh khớp wireframe {files: src/Paper.ScreenWizzard.App/Views/Capture/**, src/Paper.ScreenWizzard.App/ViewModels/Capture/**, src/Paper.ScreenWizzard.App/Resources/**, tests/Paper.ScreenWizzard.UiTests/Capture/**}

### 4. Sửa ảnh — logic và giao diện song song

- [x] T23 Tách project `Paper.ScreenWizzard.Presentation` cho khớp phương án A của ADR 0001 (Hùng nêu 2026-09-20: "app chỉ là Program.App.xaml thôi"): chuyển `App/Views`, `App/ViewModels`, `App/Resources`, `App/Mvvm` sang project mới `net10.0-windows10.0.19041.0` có `UseWPF`, tham chiếu **UseCases và Domain, không Infrastructure**; `App` chỉ còn `App.xaml`, `App.xaml.cs`, `app.manifest` (và khay, gốc ghép DI ở T17); đổi namespace `Paper.ScreenWizzard.App.*` thành `Paper.ScreenWizzard.Presentation.*`, đường dẫn pack URI của từ điển giao diện và chuỗi, `UiTests` tham chiếu Presentation; cập nhật `architecture.platformFree` của profile và `CLAUDE.md` — xong khi `build` 0 cảnh báo, `test` và `ui` **cùng số test đã chạy** như trước khi tách (128 và số hiện có), `Presentation.csproj` không tham chiếu Infrastructure (đọc lại file), và `grep` không còn `Paper.ScreenWizzard.App.Views` hay `.ViewModels` nào {files: Paper.ScreenWizzard.slnx, src/Paper.ScreenWizzard.Presentation/**, src/Paper.ScreenWizzard.App/**, tests/Paper.ScreenWizzard.UiTests/**, tests/Paper.ScreenWizzard.UnitTests/Architecture/**, .claude/paper.profile.json, CLAUDE.md}

- [x] T11 [red][unit] Test cho SPEC editor: "Hình vẽ và lịch sử" (Undo/Redo theo bước, nhánh Redo bị bỏ, đổi màu, kéo đi), "Đường thẳng, mũi tên, khung, elip" (ràng buộc Shift), "Số bước" (1,2,3; xoá 3 → 3; xoá 2 → 4), "Làm mờ" (ô 12 × 12 một màu, không khôi phục được), "Cắt" (400 × 300, dịch hình, bỏ hình ngoài, kẹp mép), "Lưu và chép" (quyết định hỏi ghi đè, JPG nền trắng), editor/F1 F2 F3 F4 F5 F6 F7 F8 F9 — xong khi đỏ ở assertion {files: tests/Paper.ScreenWizzard.UnitTests/Editor/**}
- [x] T12 [unit] Code UseCases/Domain của editor tới khi T11 xanh; verb `test` — xong khi exit 0 và số test đã chạy > 0 {files: src/Paper.ScreenWizzard.UseCases/Editor/Implements/**, src/Paper.ScreenWizzard.Domain/Editor/**, tests/Paper.ScreenWizzard.UnitTests/Editor/**}
- [x] T13 (sau T23) [red][ui] Mock trình sửa trên ảnh giả: dạ quang giữ chữ đen, mũi tên nhọn ở (300, 200), chữ "Đường ống 45° — thử nghiệm" đủ dấu, khung/đường/elip kéo ngược hướng, vị trí hình ở (150, 100) không lệch, lưu ở phóng 400% vẫn đúng cỡ, Ctrl+C không phụ thuộc mức phóng, editor/F1 F7 hiện hộp thoại và không đóng cửa sổ, editor/F6 nút mờ — xong khi đỏ ở assertion {files: tests/Paper.ScreenWizzard.UiTests/Editor/**}
- [x] T14 [ui] View, ViewModel, bộ vẽ ra pixel (`RenderTargetBitmap`) và các công cụ của trình sửa; verb `ui`, **mở ảnh ra xem** — xong khi T13 xanh và ảnh khớp wireframe {files: src/Paper.ScreenWizzard.Presentation/Views/Editor/**, src/Paper.ScreenWizzard.Presentation/ViewModels/Editor/**, src/Paper.ScreenWizzard.Presentation/Rendering/**, src/Paper.ScreenWizzard.Presentation/Resources/**, tests/Paper.ScreenWizzard.UiTests/Editor/**}
- [x] T24 Bổ sung hợp đồng trình sửa ảnh sau khi đọc báo cáo T14: `IEditorSession.SetText`, `IEditorSession.SetFontSize` và `IEditorInteractor.HitTest` (quyết định "bấm trúng hình nào" đang nằm ở `Rendering/AnnotationGeometry` trong Presentation, trái ADR 0001 mục 1; dòng SPEC editor "Chữ" về bấm đúp sửa chữ chưa có cách làm) cùng bộ khung rỗng ở `EditorSession`, `EditorInteractor` và bộ giả của test giao diện — xong khi `build` 0 cảnh báo và `test`, `ui` giữ nguyên số (221, 384) {files: src/Paper.ScreenWizzard.UseCases/Editor/Ports/**, src/Paper.ScreenWizzard.UseCases/Editor/Implements/**, tests/Paper.ScreenWizzard.UiTests/Editor/EditorFakes.cs}
- [x] T25 [red][unit] Test cho SPEC editor "Chữ" và "Số bước": `SetText` thay chữ một bước lịch sử, Undo trả chữ cũ, sửa thành chữ rỗng thì **xoá** hình (một bước); `SetFontSize` đổi cỡ của chữ và của số bước, một bước; id lạ không làm gì; và luật chọn hình: `HitTest` trả hình **vẽ sau cùng** khi hai hình chồng nhau, nét (đường, mũi tên, nét tay, viền khung và elip) trúng khi cách tối đa `tolerance` pixel, vùng làm mờ, chữ và số bước trúng khi nằm trong hộp của chúng, ngoài dung sai thì `null` — xong khi đỏ ở **assertion** {files: tests/Paper.ScreenWizzard.UnitTests/Editor/**}
- [x] T26 [unit] Code UseCases/Domain của T25 tới khi xanh; verb `test` — xong khi exit 0, số test đã chạy > 0 và không test cũ nào đỏ {files: src/Paper.ScreenWizzard.UseCases/Editor/Implements/**, src/Paper.ScreenWizzard.Domain/Editor/**, tests/Paper.ScreenWizzard.UnitTests/Editor/**}
- [x] T27 [red][ui] Test cho dòng SPEC editor "Cho chữ đã chốt, chọn bằng công cụ Chọn rồi bấm đúp → sửa lại được" (mở ô sửa với chữ cũ, chốt gọi `SetText`; chốt rỗng xoá), đổi cỡ chữ khi một chữ hay số bước đang chọn gọi `SetFontSize` cho hình đó, và bấm chọn hỏi `IEditorInteractor.HitTest` chứ không tự tính; bộ giả của test có `HitTest` và `SetText` hành xử như luật thật — xong khi đỏ ở assertion {files: tests/Paper.ScreenWizzard.UiTests/Editor/**}
- [x] T28 [ui] Code giao diện của T27 tới khi xanh; chọn hình đi qua `HitTest` của interactor, `AnnotationGeometry` chỉ còn lo vẽ khung chọn và bản sao khi kéo; verb `ui`, **mở ảnh ra xem** — xong khi T27 xanh và cả 384 test cũ vẫn xanh {files: src/Paper.ScreenWizzard.Presentation/Views/Editor/**, src/Paper.ScreenWizzard.Presentation/ViewModels/Editor/**, src/Paper.ScreenWizzard.Presentation/Rendering/**, src/Paper.ScreenWizzard.Presentation/Resources/**, tests/Paper.ScreenWizzard.UiTests/Editor/**}

### 5. Chạy thật trên Windows — sau các nhóm ui, không cùng lúc với lane ui

- [ ] T15 Tra API Win32 và WPF sẽ gọi ở `docsSource` (bảng "API đã tra" bên dưới) trước khi viết adapter — xong khi mỗi member có một dòng với trang đã đọc
- [ ] T16 [red][ui] Test adapter thật trên desktop tương tác: dựng một cửa sổ thử có các khối màu ở toạ độ biết trước rồi chụp vùng / cửa sổ và so từng pixel (không dính viền bóng); danh sách cửa sổ và màn hình; đăng ký phím tắt và phím đã bị giữ (capture/F1, shell/F7); clipboard vòng đi vòng lại; file store vào thư mục tạm chỉ đọc (capture/F4); settings file hỏng; khởi động cùng Windows ghi rồi gỡ mục của người dùng hiện tại; chỉ một bản chạy — xong khi đỏ ở assertion {files: tests/Paper.ScreenWizzard.E2eTests/Adapters/**}
- [ ] T17 [ui] Adapter Infrastructure và gốc ghép (`App.xaml.cs`, khay, manifest DPI); chạy verb `e2e` — xong khi T16 xanh và số test đã chạy > 0 {files: src/Paper.ScreenWizzard.Infrastructure/**, src/Paper.ScreenWizzard.App/App.xaml, src/Paper.ScreenWizzard.App/App.xaml.cs, src/Paper.ScreenWizzard.App/Startup/**, tests/Paper.ScreenWizzard.E2eTests/Adapters/**}
- [ ] T18 [ui] Điều khiển **chính file exe** như người dùng: bấm PrintScreen bằng `SendInput`, kéo chuột trên lớp phủ, xác nhận **hộp thoại "Đã chụp" hiện** đúng cỡ ảnh rồi bấm Sửa để ảnh mở trong trình sửa; bấm Lưu, Sao chép, Bỏ và đọc lại file, clipboard; vẽ mũi tên, số bước và làm mờ rồi lưu, đọc file lại so pixel; chụp cửa sổ và toàn màn hình; mở bản thứ hai (shell/F6); mở ảnh; **mở ảnh chụp màn hình lúc chạy ra xem** — xong khi mỗi dòng "Cho … →" của ba SPEC có một giá trị đọc lại, và capture/F7 ghi `not verifiable` kèm lý do {files: tests/Paper.ScreenWizzard.E2eTests/Drive/**}

### Last. Close

- [ ] T19 Test kiến trúc canh ADR 0001: hướng tham chiếu, Domain và UseCases nhắm `net10.0` không `-windows`, ViewModel không gọi Infrastructure, lớp trong hai tầng không nhắc `System.Windows`/`System.Drawing` — xong khi verb `test` exit 0 và số test đã chạy > 0 {files: tests/Paper.ScreenWizzard.UnitTests/Architecture/**}
- [ ] T20 `find-bug` trên ba SPEC.md, mỗi phát hiện có input được người đọc độc lập xác nhận — xong khi mỗi phát hiện đã thành test hoặc vào báo cáo
- [ ] T21 Agent `architecture-reviewer` trên các file plan này đổi (`review-files`) — xong khi đọc N/N và 0 vi phạm
- [ ] T22 Đóng ba SPEC.md (gỡ banner "Bản nháp chờ duyệt", xoá dòng bị thay), viết `CODEMAP.md` cho từng project, `check-spec` và `check-code-map` sạch, cập nhật `docs/roadmap.md` và `README.md` — xong khi hai lệnh kiểm exit 0 {files: src/**/CODEMAP.md, tests/**/CODEMAP.md, CODEMAP.md, docs/features/**/SPEC.md, docs/roadmap.md, README.md}

## API đã tra

Nguồn tra khai ở `docsSource` trong `.claude/paper.profile.json` (learn.microsoft.com, WPF và Win32). **Chưa tra:** T15 làm
việc này trước khi có dòng code nào gọi chúng; bảng dưới là danh sách sẽ tra, không phải đã tra.

| Member | Phiên bản | Trang đã đọc | Điều cần nhớ |
|---|---|---|---|
| `RegisterHotKey` / `UnregisterHotKey` | Win32, user32, Vista trở lên | [RegisterHotKey](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-registerhotkey) (2026-09-20) | trả 0 khi phím đã bị đăng ký; **cùng hWnd và id thì hotkey cũ được giữ cạnh hotkey mới, phải `UnregisterHotKey` tường minh** (nên cổng đăng ký id mới trước rồi gỡ id cũ); id 0x0000-0xBFFF; `MOD_NOREPEAT` (0x4000) chống lặp; **F12 luôn dành cho trình gỡ lỗi, không nên đăng ký**; phím có `MOD_WIN` dành cho hệ điều hành; PrintScreen của Snipping Tool có thể bị ghi đè khi cửa sổ của app đang ở foreground; hWnd NULL thì `WM_HOTKEY` vào hàng đợi thread gọi |
| `BitBlt` (`GetDC`, `CreateDIBSection` chưa tra) | Win32 GDI, gdi32 | [BitBlt](https://learn.microsoft.com/en-us/windows/win32/api/wingdi/nf-wingdi-bitblt) (2026-09-20); GetDC, CreateDIBSection: chưa tra (T15 còn mở) | tham số theo đơn vị logic; `SRCCOPY` chép thẳng, `CAPTUREBLT` thêm cả cửa sổ layered nằm trên; trả 0 khi lỗi; **lỗi nếu hai DC thuộc hai thiết bị khác nhau**, không cắt nguồn |
| `DwmGetWindowAttribute` với `DWMWA_EXTENDED_FRAME_BOUNDS` | Win32 DWM, dwmapi | [DwmGetWindowAttribute](https://learn.microsoft.com/en-us/windows/win32/api/dwmapi/nf-dwmapi-dwmgetwindowattribute) (2026-09-20); trang enum `DWMWINDOWATTRIBUTE` chưa đọc (T15 còn mở) | trả HRESULT, `S_OK` là thành công; kiểu và cỡ giá trị theo từng cờ (RECT cho EXTENDED_FRAME_BOUNDS) nằm ở trang enum, **phải đọc trước khi gọi** |
| `EnumWindows` (`IsWindowVisible`, `IsIconic`, `DWMWA_CLOAKED` chưa tra) | Win32 user32 | [EnumWindows](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-enumwindows) (2026-09-20); còn lại: chưa tra (T15 còn mở) | chỉ duyệt cửa sổ top-level của ứng dụng desktop (Windows 8 trở lên); callback trả FALSE thì dừng; **trang không nói thứ tự z**, nên thứ tự "trên cùng trước" phải được test thật ở T16, không được giả định |
| `EnumDisplayMonitors` (`GetMonitorInfo`, `MonitorFromPoint`, `GetDpiForMonitor` chưa tra) | Win32 user32 | [EnumDisplayMonitors](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-enumdisplaymonitors) (2026-09-20); còn lại: chưa tra (T15 còn mở) | hdc NULL và clip NULL thì liệt kê **mọi** màn hình, toạ độ là toạ độ desktop ảo (có thể âm); callback nhận HMONITOR |
| `SetProcessDpiAwarenessContext`, manifest `PerMonitorV2` | Win32 user32, Windows 10 1703 trở lên | [SetProcessDpiAwarenessContext](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setprocessdpiawarenesscontext) (2026-09-20) | **Microsoft khuyên khai bằng manifest, không gọi API** (T1 đã khai `PerMonitorV2`); gọi API sau khi manifest đã đặt thì lỗi `ERROR_ACCESS_DENIED`; mặc định là UNAWARE nếu không đặt |
| `Clipboard.SetImage` / `SetDataObject` | WPF | chưa tra (T15) | có thể ném `COMException` khi bị khoá |
| `RenderTargetBitmap`, `DrawingVisual`, `FormattedText` | WPF | chưa tra (T15) | DPI của bitmap ra; font tiếng Việt |
| `NotifyIcon` | Windows Forms | chưa tra (T15) | dùng từ WPF, dọn `Dispose` khi thoát |
| `Registry` `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` | .NET | chưa tra (T15) | khoá của người dùng hiện tại, không cần quyền cao |
| `Mutex` đặt tên | .NET | chưa tra (T15) | `Local\` cho từng phiên đăng nhập |

## Bằng chứng

Mỗi task đã tick có đúng một dòng ở đây; task phủ `F<n>` ghi tên test `F<n>_…` của từng mã. Verdict là một trong ba:
`pass`, `fail`, `not verifiable`.

| Task | Lệnh / id / giá trị đọc lại | Verdict |
|---|---|---|
| T1 | `paperflow.ps1 build` → `dotnet build Paper.ScreenWizzard.slnx`: 7 project, **0 Warning(s), 0 Error(s)**, exit 0. Lần đầu đỏ vì WFO0003 (bộ phân tích WinForms đòi bỏ khai báo DPI khỏi manifest) → tắt riêng mã đó trong App.csproj kèm lý do. Gói: NUnit 4.6.1, NUnit3TestAdapter 6.3.0, Test.Sdk 18.10.1, FlaUI 5.0.0 = bản mới nhất trên nuget.org 2026-09-20. Verbs `build`, `test`, `ui`, `e2e` khai trong profile; lane `e2e` bỏ (xem Decisions) | pass |
| T2 | `paperflow.ps1 build`: 0 Warning(s), 0 Error(s), exit 0. Hợp đồng: Domain (Geometry, Capture, Common, Shell, Editor), UseCases Common (11 cổng + `IImageDelivery`), Shell (`IShellInteractor` + 5 cổng), Capture (`ICaptureInteractor`, `ICaptureSession` + 3 cổng), Editor (`IEditorInteractor`, `IEditorSession`). Bộ khung `ShellInteractor`, `CaptureInteractor`, `EditorInteractor`, `EditorSession`, `ImageDelivery` trả giá trị rỗng. Grep `System.Windows`, `System.Drawing`, `Microsoft.Win32`, `InteropServices` trong `src/`: chỉ ở project App (`App.xaml.cs`, `CommandBase.cs`), không ở Domain/UseCases | pass |
| T3 | lane-logic, verb `test` lọc `Shell` trên bộ khung rỗng: 46 chạy, **36 đỏ ở assertion** (mẫu: `Expected NeedsModifier, But was None`; `Expected PixelPoint { X = 1504, Y = 16 } But was { X = 0, Y = 0 }`; `Expected "Shell.SettingsNotSaved" But was null`), 0 đỏ vì build hay ngoại lệ. 10 test xanh ngay lần đầu (bộ khung trả rỗng): agent phá từng luật trong code sản phẩm và thấy đỏ ở assertion (bỏ qua `IsFirstInstance`; `CheckSaveFolder` không bao giờ trả Invalid; ngôn ngữ hệ thống luôn tiếng Việt; bỏ qua ngôn ngữ chọn tay), rồi trả code về. Tên test theo mã: `shell/F1_CorruptSettingsFileStartsWithDefaultsAndANotice`, `F2_SettingsThatCannotBeWrittenAreReportedAndStillUsedThisSession`, `F3_AutostartEntryThatCannotBeWrittenRevertsTheSwitchWithTheReason`, `F4_ChangedHotkeyHeldByAnotherProgramKeepsTheOldOne`, `F5_MissingSaveFolderIsOfferedForCreationAndNothingIsCreatedYet`, `F6_SecondLaunchMakesTheFirstCopyShowItsCaptureBar`, `F7_HotkeyThatCannotBeRegisteredAtStartIsNamedAndTheOthersStay`. Số đỏ do lane agent báo, mình không tái dựng bộ khung để đếm lại | pass |
| T4 | mình chạy lại `paperflow.ps1 test`: **Total 46, Passed 46, Failed 0**, exit 0. Đổi tên `F9AloneIsAccepted` thành `FunctionKeyF9AloneIsAccepted` vì tên bắt đầu bằng `F9` dễ bị đọc là mã F9 của SPEC. Các dòng "Cho …" không có test đơn vị, chuyển cho lane khác: thanh chụp đóng vẫn ở khay (T18), đổi ngôn ngữ tức thì, Tab/Enter/Esc/tương phản (T5, T6), đăng nhập lại chạy ẩn (T16, T18) | pass |
| T5 | lane-ui, verb `ui` trên bộ giao diện rỗng: **64/64 đỏ, tất cả là assertion của NUnit**, không có NullReference hay lỗi build (mẫu: `'HotkeyBox.Rectangle' never appeared in the window`; `keys the shell needs and Strings.vi.xaml lacks`; `Brush.Text on Brush.Window: brush missing`; `no window with AutomationId 'ToastWindow' opened`). Lần đỏ đầu có 3 test xanh vì lý do sai (nút thanh tiêu đề bị đếm là nút của cửa sổ, kéo nhầm thanh tiêu đề, một chuỗi khoá lạ bị bỏ qua): agent siết lại rồi được 64/64 đỏ. `shell/F4_…`, `F5_…` nằm trong `SettingsWindowTests` | pass |
| T6 | mình chạy cuối nhóm: `build` **0 Warning(s), 0 Error(s)**; `test` **46/46**; `ui` **65/65** (64 của lane + 1 test mình thêm), 4 lần liên tiếp xanh, 6 trong 8 lần gần nhất xanh. Mình mở `shell-settings-vi-dark.png` (hai cột, cả hai giao diện đọc được, ô chất lượng JPG mờ khi chọn PNG) và `shell-capturebar-vi-light.png` (bốn nút Vùng / Tự do / Cửa sổ / Cả màn, đường ngăn, ⚙ và ✕), khớp wireframe; lane agent đã mở thêm 12 ảnh khác (thanh chụp vi/en, Cài đặt vi/en, F4, F5, toast, hộp lỗi tối; `shell-error-light.png` **chưa ai mở**). **Lỗi thật mình tìm thấy khi đọc T6:** giao diện gọi `Apply` với giá trị cũ của "Khởi động cùng Windows" rồi `SetAutostart` (không tự lưu), nên công tắc Windows đã nhận vẫn không được ghi vào file. Viết test đỏ `AutostartSwitchWindowsAccepted_IsWhatTheSettingsFileGets` (đỏ ở `Expected: True But was: False`), đổi thứ tự trong `SaveSettingsCommand` (hỏi Windows trước, rồi `Apply`), xanh; `F3_AutostartFails_…` vẫn xanh. **Không kiểm được:** khay hệ thống chưa có hình (chỉ mô hình view, NotifyIcon ở T17); không test nào chứng minh cổng `SystemParameters.ClientAreaAnimation` của toast; phím PrintScreen thật không bao giờ được gửi. **Test chập chờn do môi trường:** `Bar_DraggedByItsBackground_MovesTheWindow` đỏ 3/6 lần chạy riêng vì đòi cửa sổ lên foreground trong khi thanh chụp cố ý không kích hoạt; đã bỏ dòng đòi foreground thừa (test chỉ dùng chuột), riêng 0/6 đỏ; `EnsureForeground` thử tối đa 4 lần thay vì 1. Trong chạy đủ vẫn từng có 1 lần đỏ sau đó (không xác định được nguyên nhân); các lần chạy sau xanh. Máy đang được dùng cho việc khác nên lane `ui` cần màn hình rảnh | pass |
| T7 | lane-logic, verb `test` lọc `UnitTests.Capture.` trên bộ khung rỗng: 82 chạy, **81 đỏ ở assertion, 0 đỏ vì ngoại lệ hay build**, 1 xanh ngay lần đầu (mẫu: `BeginAsync gave no session (issue None) Expected: not null But was: null`; `F4: Assert.That(missing.KeepDialogOpen, Is.True)`; `F9: Expected: OutOfMemory But was: None`). Test theo mã: `capture/F2_ARegionUnderThreeByThree…`, `F3_AnOutlineWithFewerThanThree…`, `F4_SaveIntoAFolderThatIsMissingOrReadOnly…`, `F5_ABusyClipboard…`, `F6_AMonitorChangeWhileChoosing…`, `F8_ANewCaptureCancels…`, `F9_AnImageTooLargeToHold…`. F1 và F7 của capture thuộc nhóm 5, F10 thuộc lane ui. Số đỏ do lane agent báo | pass |
| T8 | mình chạy lại `paperflow.ps1 test`: **Total 128, Passed 128, Failed 0** (46 shell + 82 capture), exit 0. Test xanh ngay lần đầu chứng minh không rỗng: `RequestFor` đổi thành `!IncludeCursor` → đỏ (`Expected IncludeCursor = False But was True`); ba phép phá thêm, đều trả về: bỏ kiểm thế hệ F8 (đỏ, sau khi thêm `Task.WhenAny` cho khỏi treo), ngưỡng vùng nhỏ 2 thay 3 (F2 đỏ), bỏ nền trắng JPG (3 test đỏ). Mình đọc lại mã: `!IsCurrent(mine)` và `MinimumRegionSide = 3` đã trả đúng. Số giả định lane logic tự chọn: đường bao tự cắt (hình số 8) tính diện tích tuyệt đối shoelace nên có thể ra 0; đường bao hoàn toàn ngoài màn hình, cửa sổ không có phần nào trên màn hình → `Failed`; F9 khi cắt dùng giả lập ảnh 30000×30000 vì không cấp phát thật. **Hở:** `DeliveryIssue` không có giá trị hết bộ nhớ, nên `ImageDelivery` không bắt `OutOfMemoryException` khi mã hoá ảnh rất lớn lúc lưu; F9 hiện chỉ phủ lúc chụp và cắt. Cần một task nhỏ và một giá trị mới trong hợp đồng nếu muốn phủ cả lúc lưu | pass |
| T9 | lane-ui, verb `ui` lọc `ScreenCapture` trên bộ giao diện rỗng: 140 chạy, **138 đỏ ở assertion NUnit, 0 ngoại lệ**, 2 xanh (`Messages_AnUnknownKey_NeverRendersBlank` vi và en, kiểm `LanguageService.Format` đã có). Mẫu: `Expected: PixelSize { Width = 450, Height = 300 } But was: PixelSize { Width = 0, Height = 0 }`, `'SaveButton' never appeared in the window`, `Capture.RegionTooSmall has no Vietnamese text`. Lần đỏ đầu có 10 test đỏ vì ngoại lệ phía test và 5 test xanh vì lý do sai: agent đã sửa. Các vỏ rỗng của view model phải đặt trong `src/App/...Capture`, ngoài glob T9 nhưng trong glob T10 | pass |
| T10 | mình chạy cuối nhóm: `build` **0 Warning(s), 0 Error(s)**; `test` **128/128**; `ui` **205/205** (65 shell + 140 capture), lane agent chạy 3 lần liên tiếp đều 205/205. Ảnh lane agent đã mở và phán xét: lớp phủ khi kéo chữ nhật (khung, vùng sáng, nhãn "300 × 200"), hover cửa sổ ("Notepad" và khung), vùng tự do, F2, F3, đếm ngược tối, hộp thoại "Đã chụp" vi sáng / vi tối / en sáng, F4 sáng, F5 tối, luồng chụp; lỗi ảnh tìm ra và sửa: gợi ý "Esc / chuột phải: huỷ" và thông báo F2 đè lên nhãn tên cửa sổ, nay ở dưới trái theo wireframe. Chưa ai xem: `capture-countdown-light`, `capture-done-en-dark`, `capture-overlay-labels-light` (chỉ đo tương phản). Lỗi lane agent tìm khi làm xanh: `DisplayUnits.ToPixels(300, 200)` dính nhầm overload hằng số (nay `SizeToPixels`); hộp thoại chưa căn giữa màn hình chụp vì kích thước chỉ chốt ở `ContentRendered`. **Lỗi mình tìm khi đọc báo cáo:** `Brush.Focus` trùng `Brush.Accent` và vòng focus vẽ trong khung nút, nên trên nút Accent (nút Lưu của Cài đặt, nút OK của hộp lỗi) vòng focus **là chính nền nút, Tab vô hình**; test focus cũ chỉ đo nút Huỷ nền xám. Viết `Keyboard_FocusedAccentButton_ShowsARingThatIsNotTheFillColour` (sáng và tối), đỏ ở `Expected: greater than 20 But was: 0`; thêm vòng trong màu `Brush.Window` vào mẫu nút, xanh; 8 test focus xanh. **Không kiểm được:** máy này 100% DPI, 150% chỉ được chứng minh bằng test thuần `DisplayUnits` (450×300), chưa có cửa sổ thật ở 150% và chưa thử cửa sổ vắt qua hai màn hình; `OnDpiChanged` chưa chạy; `CaptureInteractor` thật chưa được nối vào các view model (T17, T18); Esc cần lớp phủ ở foreground, có thể bị Windows từ chối trên đường phím tắt thật | pass |
| T11 | lane-logic, verb `test` lọc `UnitTests.Editor.` trên bộ khung rỗng: 93 chạy, **87 đỏ ở assertion**, 6 xanh (mẫu: `F5_*: Assert.That(outcome.Message, Is.Not.Null) Expected: not null But was: null`; `F6_*: session.CanUndo Expected: True But was: False`; `F2_*: Issue Expected: NotAnImage But was: None`; làm mờ 200×100: `wrong Expected: 0 But was: 20000`). Lần chạy đầu có 5 ngoại lệ chỉ số phía test, đã sửa bằng assert số phần tử trước. 6 test xanh (F4 ×4 vì bộ khung trả `false`, hai test tầm thường): agent viết lại F4 để trước tiên thêm một chữ thật, và phá luật cho hai test kia đỏ. Test theo mã: `editor/F1_ASaveThatFails…`, `F2_AFileThatIsNotAnImage…`, `F3_PastingWhenTheClipboardHasNoImage…`, `F4_CommittingATextWithNothingTyped…`, `F5_ACropSmallerThanOnePixel…`, `F6_UndoAndRedoWithNothingToDo…`, `F7_ClosingWithUnsavedChangesAsks…`, `F8_AnImageTooLargeToHold…`, `F9_WhenTheOriginalWasChangedOrRemoved…`. Số đỏ do lane agent báo | pass |
| T12 | mình chạy lại `paperflow.ps1 test`: **Total 221, Passed 221, Failed 0** (128 + 93), exit 0. 14 phép phá luật, đều đỏ ở assertion rồi trả về (ô làm mờ 12→11: 4 đỏ; bỏ kiểm chữ rỗng: 3; `_savedIndex` khởi tạo -1: 5; không bỏ nhánh Redo: 2; số bước = đếm+1: 1; bỏ dịch hình khi cắt: 4; snap Shift: 2; tắt kiểm F9: 3; `AskOverwriteOrCopy` không bao giờ thành `WriteDirect`: 3; `DecideClose` luôn `Close`: F1 và F7 đỏ; bỏ `MarkSaved`: 3; `IsDirty` luôn false: 10; làm mờ ghi vào mảng nguồn: 1; kiểm "chạm" khi cắt: một phép phá sống sót lần đầu nên thêm hai bản vẽ (chỉ sát mép trái, chỉ nằm trên) rồi mới đỏ). Mình đọc lại mã `EditorSession` và `EditorInteractor`: không còn dấu vết phép phá. **Hợp đồng đổi:** `EditorInteractor` nhận thêm tham số thứ sáu `IClockPort clock` (thứ tự: files, codec, clipboard, delivery, clock, log); gốc ghép DI ở T17 phải truyền đồng hồ. `SaveDecision` không có chỗ chứa thông báo nên F9 trả `FileChangedOnDisk` kèm `Path` của file gốc và gợi ý Lưu thành, và lane ui dựng `Editor.FileChangedOnDisk(path)` từ đó. **Giả định lane logic tự chọn:** snap Shift của đường/mũi tên là **chiếu** hướng kéo lên hướng đã snap (số ví dụ (100,100)→(300,120) cho (300,100) mà chiều dài Euclid cho (301,100), hai điều mâu thuẫn trong lời giao việc của mình nên agent theo con số; (295,295) và kéo ngược (110,110)); sau `Save` thành công, đường dẫn đã lưu thành `SourcePath` nên Ctrl+S ghi thẳng cả với "Lưu thành" bản mới; lưu hỏng không tính là đã đồng ý ghi đè; kiểm F9 chạy ở cả hai trạng thái; "chạm" khi cắt: hộp bản vẽ đệm bằng độ dày (nét), `FontSize` (số bước), và độ rộng chữ ≈ dòng dài nhất × `FontSize` (chữ), nghiêng về giữ lại bản vẽ, bộ vẽ cắt sau; `Move(0,0)` vẫn là một bước; id lạ thì không làm gì và không có bước lịch sử; ô làm mờ đếm từ góc trên trái của vùng kể cả khi nằm ngoài ảnh, trung bình làm tròn gần nhất, không làm mờ thì `RenderBase` trả chính `Source`. **Không kiểm ở mức này (lane ui):** dạ quang đè chữ đen, đầu mũi tên, chữ Việt và dấu độ, vị trí ở 100% và 400%, Ctrl+C mọi mức phóng, phím trong ô chữ, chọn bản vẽ so với đổi màu bản vẽ kế | pass |
| T13 | lane-ui, verb `ui` lọc `ImageEditor` trên vỏ rỗng: 172 chạy, **170 đỏ ở assertion NUnit, 0 ngoại lệ**, 2 xanh ngay lần đầu là test đối chứng (`Flatten_NoDrawings_IsTheImageItself`, `Thickness_StaysBetween1And20`) đã được chứng minh không rỗng: lật một bit trong flatten làm đỏ, kẹp độ dày thêm 1 làm đỏ, rồi trả code. Mẫu: `'ToolButton.Select' never appeared in the window`; `a fresh image has no step to undo Expected: False But was: True`; `nothing was painted on the image`; `editor keys missing from Strings.vi.xaml`. Lần đầu có một lần sập host (gọi UIA Close từ chính luồng UI) và 6 test xanh vì lý do sai: đã sửa và siết. Đó là các cửa sổ trống nhấp nháy mà Hùng thấy trong lúc lane chạy bước đỏ | pass |
| T14 | mình chạy cuối nhóm: `build` **0 Warning(s), 0 Error(s)**; `test` **221/221**; `ui` **384/384** (207 cũ + 177 mới, gồm 4 test trên `EditorSession` thật của lane logic: Undo/Redo, kéo là một bước, ô mosaic 12×12, cắt 400×300). Lane agent chạy đủ 5 lần: 4 xanh, 1 đỏ ở `Text_ClickTypeAndCtrlEnter` (`TextDraftBox never appeared`), không tái hiện được; agent thêm assertion để lần sau chỉ rõ đỏ ở công cụ hay ở cú bấm, 3 lần sau xanh. Mình mở `editor-vi-light.png`: hai hàng công cụ, bảng màu 8 màu, ô hex, thanh độ dày, cỡ chữ, hoàn tác/làm lại (làm lại mờ), Vừa khung / 1:1 / 100%, ảnh với khung đỏ, số bước 1-3, dạ quang vàng, mũi tên, elip xanh, khung chọn nét đứt, chữ "Đường ống 45° — thử nghiệm" đủ dấu, thanh trạng thái "760 × 440  Chưa lưu" và ba nút Sao chép / Lưu thành… / Lưu. Lane agent mở thêm: tối, en tối, cắt, thu phóng 300%, chữ Việt cận cảnh, hộp chữ nháp, hộp hỏi khi đóng, hộp hỏi ghi đè; lỗi tìm ra từ ảnh và sửa: thanh cuộn hệ thống sáng chói trên nền tối (viết lại kiểu `ScrollBar`), nút "100%" trùng nhãn mức phóng (đổi thành "1:1"), ô cỡ chữ bị cắt; `editor-en-light` chưa ai mở. **Hở phát hiện, đã tách task:** (1) chưa làm "bấm đúp sửa chữ" và đổi cỡ chữ hình đang chọn của SPEC; (2) luật chọn hình nằm ở Presentation: T24-T28. **Chưa xử lý, ghi nợ:** cửa sổ trình sửa dùng `CenterScreen` và rộng 1120 đơn vị nên trên màn phụ 200% của máy này nhô ra ngoài vùng làm việc (chưa kẹp bằng `MonitorFromWindow`/`GetMonitorInfo`, chưa tra), test ghim về màn chính 100%; kéo thanh độ dày khi đang chọn hình tạo một bước lịch sử cho mỗi nấc (nên gộp); nếu màu hình đang chọn không nằm trong bảng thì nhóm ô màu mất điểm dừng Tab; kéo thả file chỉ kiểm `AllowDrop` và lệnh mở; mosaic thật không hiện trong ảnh chụp (session giả), chỉ được chứng minh bằng test điểm ảnh; dạ quang trong cửa sổ vẽ bằng alpha thường còn file ra nhân màu, nên chữ đen dưới dạ quang hơi ngả ô-liu trên màn hình. API: đã đọc `RenderTargetBitmap`, `FormattedText` (ctor có `pixelsPerDip`), `DrawingVisual.RenderOpen`, `DrawingContext.DrawImage`, `Pen.LineJoin`/`StartLineCap`, `StreamGeometry`, `PixelFormats.Pbgra32`, `BitmapSource.CopyPixels`, `Window.Closing`, `DataFormats.FileDrop`, `UIElement.Drop`, `MouseWheelEventArgs.Delta`, `Keyboard.Modifiers`, `ScrollViewer`, `FrameworkElementAutomationPeer`; **chưa tra:** `DrawGeometry`, `DrawEllipse`, `DrawRoundedRectangle`, `DrawText`, `DrawLine`, `PushTransform`, `PushClip`, `CombinedGeometry`, `DashStyle`, `RenderOptions.SetBitmapScalingMode`, `Rect.Intersect`/`Inflate`, `CaptureMouse`, `LostMouseCapture`, `Dispatcher.BeginInvoke`, `Keyboard.Focus`, mẫu `Slider`/`Track`/`Thumb`, `Window.ShowDialog`/`Owner`/`DialogResult`, `AutomationProperties` | pass |
| T24 | mình thêm hai thành viên `SetText` và `SetFontSize` cho `IEditorSession`, `HitTest(session, point, tolerance)` cho `IEditorInteractor`, bộ khung rỗng ở `EditorSession` và `EditorInteractor`, bản rỗng ở `FakeEditorSession` và `FakeEditorInteractor` của test giao diện để chúng biên dịch. Lần build đầu đỏ đúng 3 lỗi `CS0535` ở bộ giả (chưa cài thành viên mới); sau khi thêm: `build` **0 Warning(s), 0 Error(s)**; `test` **221/221**; `ui` **384/384**, không đổi | pass |
| T25 | lane-logic, verb `test` lọc `Editor.HitTestTests` và `Editor.TextEditTests` trên bộ khung rỗng của T24: 58 test, **44 đỏ ở assertion**, 0 đỏ vì build hay ngoại lệ (mẫu: `Expected: 00000001-0000-0000-0000-000000000000 But was: null`; `Expected string length 5 but was 9 ("Van 2" vs "Đường ống")`; `Expected: 6 But was: 18` ở kẹp cỡ chữ). 14 test xanh ngay lần đầu là ca "không trúng gì / không làm gì": agent phá code sản phẩm ba nhóm phép phá (phiên: id lạ đẩy một bước, sửa chữ trên hình khác loại, `SetText` xoá vùng chọn, kẹp 300, bỏ kiểm cùng cỡ, `SetFontSize` nhận hình chữ nhật; trúng: trong ruột khung, nét rỗng, trong ruột elip; `HitTest` trả `Guid.Empty` khi hụt) và thấy đỏ ở assertion rồi trả về. Số đỏ do lane agent báo | pass |
| T26 | mình chạy lại `paperflow.ps1 test`: **Total 279, Passed 279, Failed 0** (221 + 58), exit 0; quét mã không còn `false &&` hay `Guid.Empty`. `HitTest` là quét tuyến tính từ hình vẽ sau cùng, dừng ở hình đầu tiên trúng, chi phí O(số điểm trên ảnh) không cấp phát (ghi trong comment). Luật lane logic chọn: elip là dải giữa hai elip cùng tâm nới và co bán trục bằng `tolerance + thickness/2` (đúng trên trục, lệch tối đa 1-2 px ở 45 độ; elip nhỏ tới mức bán trục co ≤ 0 thì cả elip tính là nét); viền hình chữ nhật, so sánh `<=`; hộp chữ, số bước, làm mờ trúng khi trong hộp nới `tolerance` (hộp số bước là hình vuông nửa cạnh `FontSize`, không phải hình tròn); nét một điểm là đĩa bán kính r; nét không có điểm không bao giờ trúng. **`SetText` cùng chữ cũ vẫn là một bước lịch sử**; `SetFontSize` cùng cỡ thì không (giao diện muốn không có bước phải tự bỏ lời gọi). `SetText` rỗng trên hình không phải chữ không xoá gì; `null` coi như rỗng | pass |
| T27 | lane-ui, verb `ui` lọc `EditorTextEditAndSelectTests` trên mã sản phẩm chưa đổi: 18 chạy, **16 đỏ ở assertion NUnit, 0 ngoại lệ, 0 lỗi build** (mẫu: `the double-click opened the text box: Expected True But was False`; `Expected is (id,30)[1], actual is List with 0 elements` ở `SetFontSize`; `SetThickness Expected: 1 But was: 11`; `HitTests Expected Count 1 But was 0`; `Select ... Expected: null But was: 4210ede6-…`, cửa sổ vẫn tự tính hit). 2 xanh là ca đối chứng (bấm đúp lên hình chữ nhật không mở ô; ô cỡ chữ khi không chọn gì chỉ đặt cỡ kế) | pass |
| T28 | mình chạy cuối: `build` **0 Warning(s), 0 Error(s)**; `test` **279/279**; `ui` **431/431** (384 + 47 mới), lane agent chạy hai lần liên tiếp cũng 431. Lane agent phá code ba kiểu (`SetText` luôn được gọi, cỡ chữ không đẩy khi bấm chỗ khác, `SetFontSize` cùng cỡ), ba nhóm test đỏ rồi trả về; mở `editor-text-edit-vi-light`, `-dark`, `editor-text-edited-vi-light`: ô sửa nằm đúng chỗ chữ với chữ cũ và con trỏ ở cuối, chữ đã chốt không bị vẽ hai lần, thanh trạng thái "Ctrl+Enter hoặc Esc: chốt chữ", cả hai giao diện đọc được; chữ cỡ 28 không bị cắt trong khung chọn nét đứt. Mình đọc lại mã: `EditorViewModel` gọi `Interactor.HitTest(...)`, `AnnotationGeometry` chỉ còn `BoundsOf`, `Translate`, `Normalize` (không còn mã hit). Hành vi lane chọn: bấm đúp là lần nhấn thứ hai `ClickCount == 2`; Esc trong ô chốt chữ như công cụ Chữ (SPEC "Esc chốt chữ"); ô không đổi thì không gọi `SetText` (không có bước lịch sử), ô rỗng gửi `SetText(id, "")`; ô cỡ chữ chờ Enter hoặc mất tiêu điểm rồi đẩy một `SetFontSize` cho hình đang chọn lúc bắt đầu gõ; thanh độ dày: nhấn và thả núm là một `SetThickness` (xem trước trực tiếp), phím mũi tên và bấm lên thanh vẫn một bước mỗi lần; dung sai chọn `ceil(4 / ViewScale)` pixel ảnh. Chữ dịch khoảng 4 px phải, 2 px xuống khi mở ô do đệm ô (giống công cụ Chữ). **Không kiểm được:** bộ giả `FakeEditorInteractor` ước lượng cỡ hộp chữ không có phông và `EditorWithRealSessionTests` vẫn dùng interactor giả nên luật `HitTest` thật chỉ được test đơn vị chạy. API: đã đọc `MouseButtonEventArgs.ClickCount`, `Thumb.DragStarted/DragCompleted` (và sự kiện định tuyến), `DragCompletedEventArgs`, `UIElement.AddHandler(RoutedEvent, Delegate, bool)`, `TextBox.CaretIndex`; chưa tra `DragStartedEventHandler`, `DragCompletedEventHandler`, `Key.Return`, `KeyEventArgs.Key`, và lời gọi FlaUI `Mouse.DoubleClick`, `Keyboard.Type(string)` | pass |
| T23 | Tách `Paper.ScreenWizzard.Presentation` bằng `git mv` (giữ lịch sử) `App/Views`, `ViewModels`, `Resources`, `Mvvm`; đổi namespace `Paper.ScreenWizzard.App.*` thành `Paper.ScreenWizzard.Presentation.*` (66 file), pack URI `/Paper.ScreenWizzard;component/` thành `/Paper.ScreenWizzard.Presentation;component/`, hai hằng tên assembly, đường dẫn tham chiếu của `UiTests`; `App` chỉ còn `app.manifest`, `App.xaml`, `App.xaml.cs`, `csproj`. **Trước và sau, cùng số test:** `build` 0 Warning(s) 0 Error(s); `test` **128/128**; `ui` **207/207**. `Presentation.csproj` chỉ có `ProjectReference` tới UseCases và Domain (đọc lại file), không Infrastructure; `grep` không còn `Paper.ScreenWizzard.App.(Views|ViewModels|Mvvm|Resources)`. Lần `ui` đầu sau khi tách đỏ 2 test (`Keys_EveryDynamicResourceOfTheCaptureViews_IsDefined`, `Keys_TheCaptureViewsCarryNoHardCode…`) vì chúng quét thư mục nguồn theo đường dẫn cũ và đỏ chứ không im lặng bỏ qua; sửa đường dẫn, xanh. Profile: `architecture.platformFree` cấm `Paper.ScreenWizzard.Infrastructure` trong `**/Paper.ScreenWizzard.Presentation/**`. Khay và gốc ghép DI vẫn là việc của T17 | pass |

# <Tên việc — một câu, lời người dùng> — YYYY-MM-DD

<!--
Khuôn này do skill `spec` sở hữu. Brief là bản ghi của MỘT lần được yêu cầu: ngắn, lời người dùng,
đóng băng ngày bắt đầu làm — sau đó không ai sửa nó.

  - KHÔNG tên class/method/file/test, KHÔNG task, KHÔNG dòng Trạng thái: trạng thái duyệt nằm ở plan,
    và cổng `tasks` chỉ đọc plan
  - được phép ở đây, và chỉ ở đây: model/dữ liệu thật, id phần tử, ảnh chụp của ngày hôm đó — những thứ
    không bao giờ vào SPEC.md
  - luật không viết ở đây: nó ở SPEC.md, brief chỉ nói mục nào của SPEC.md đổi
-->

<Ai yêu cầu, ngày nào, bằng lời của họ: họ thấy gì sai hoặc thiếu, muốn gì, và vì sao. Số đo nếu có.>

## Tài liệu đi kèm

- <video/ảnh/file dữ liệu người dùng đưa, đã chép nguyên vẹn vào `input/`>
- <model hay dữ liệu làm lộ ra vấn đề, và cách tìm lại nó>

## SPEC.md đổi ở đâu

- <mục mới hoặc mục bị đổi, theo tên heading trong SPEC.md, mỗi mục một dòng: đổi gì, bằng lời người dùng>

## Được đụng tới trong đợt này

- <model/dữ liệu và id phần tử được phép đọc, sửa, tạo thử rồi dọn; "không có" nếu chỉ chạy unit>

---

Luật: [SPEC.md](SPEC.md) · Việc làm và bằng chứng: [YYYY-MM-DD-<task>-plan.md](YYYY-MM-DD-<task>-plan.md)

namespace Paper.ScreenWizzard.App;

/// <summary>
/// The composition root. T1 only stands the application up; the tray, the hotkeys and the DI container arrive with
/// the adapters (plan T17).
/// </summary>
public partial class App : System.Windows.Application
{
}
